// App.jsx
// Root component. Replace Vite's default App.jsx with this file.
// All it does is render ShipmentForm — you can add routing here later
// (e.g. React Router) when you add more pages.

import ShipmentForm from './components/ShipmentForm';

export default function App() {
  return <ShipmentForm />;
}
